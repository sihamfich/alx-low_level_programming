#include <stdlib.h>
#include <stdio.h>
/**
 * mul - function that multiply tow integer
 * @a: input
 * @b: input
 * Return: multiplication of two numbers
*/
int mul(int a, int b)
{
	return (a * b);
}
